# Dockerfile 改进
我已经修改了backend/Dockerfile文件，采取了以下改进措施：
更换基础镜像：从ubuntu24.04改为更稳定的ubuntu22.04
简化依赖安装：使用更通用的包名（如python3而不是python3.12）
优化构建过程：合并相关的RUN命令以减少层的数量
标准化命令：统一使用python3和pip3命令
更新入口命令：将最后的CMD命令中的python改为python3